sudo: false

language: node_js

node_js:
  - "6"
  - "7"
  - node

dist: trusty

cache: yarn

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

before_install:

install:

before_script:
  - yarn install

script:
  - yarn test
  - yarn e2e
  - yarn build:prod
